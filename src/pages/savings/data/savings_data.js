
export const newSavingsInstance = (age1, color, reg, stream, age2, transaction, taxType, value) => ({                                              //this is the initial state of income being stored to the reducer.            
        age1,
        color, 
        reg,
        stream,
        age2,
        transaction,
        taxType,
        value,
    })

