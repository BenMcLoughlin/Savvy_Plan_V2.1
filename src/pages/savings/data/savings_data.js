
export const savingsInstance_data = (fromAge, id, reg, stream, toAge, transaction, value) => ({                                              //this is the initial state of income being stored to the reducer. 
        color: '#D8BABB',
        fromAge,
        reg,
        stream,
        toAge,
        id,
        transaction,
        value,
    })

