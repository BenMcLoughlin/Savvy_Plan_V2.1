import React, { Component } from 'react'
import { TaxPopupStyled } from "./TaxPopupStyles"

export default class TaxPopup extends Component {
    render() {
        return (
            <TaxPopupStyled>
                TAX POPUP HELLOW~
            </TaxPopupStyled>
        )
    }
}
