import styled from "styled-components"
import {BackgroundLight} from "../../Shared/Styles"

export const MainStyled = styled.div`
    grid-area: m;
    ${BackgroundLight}
    display: grid;
    height: 100%;
    width: 100%;
`